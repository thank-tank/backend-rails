<%= form_with(model: @post, local: true) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :content, placeholder: "Drop some gratitude..." %>
	</div>
	<%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
